export interface AchievementBadgeProps {
  slug: string
  title: string
  iconUrl?: string
  earnedAt: string
}

export default function AchievementBadge(_props: AchievementBadgeProps) {
  return null
}
