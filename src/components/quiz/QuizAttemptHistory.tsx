import type { QuizAttempt } from '@/types/quiz'

export interface QuizAttemptHistoryProps {
  attempts: QuizAttempt[]
}

export default function QuizAttemptHistory(_props: QuizAttemptHistoryProps) {
  return null
}
